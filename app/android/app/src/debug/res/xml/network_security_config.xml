<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9 and later block cleartext HTTP by default. Without this, a http:// base URL fails
  with an error that reads like a networking or server bug rather than a policy decision.

  This file lives in src/debug, so it is compiled into debug builds only. Release builds keep
  the platform default and must talk HTTPS, which is the main reason for terminating TLS at
  the VPS at all.

  Scoped to specific hosts rather than a blanket usesCleartextTraffic="true":
    10.0.2.2   the host machine as seen from the Android emulator
    localhost  a device forwarding a port with `adb reverse`
  A physical phone on the same WiFi needs its host's LAN IP added here.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
