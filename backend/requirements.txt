# Production pins.
#
# Install torch from the CPU index, NOT the default:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# The default wheel bundles ~2.5 GB of CUDA libraries the VPS has no GPU to use. Same
# version Colab reported, different build.
#
# The ML pins below are the exact versions from models/model_facts.json. A .pt file stores
# tensors plus the module paths of the classes that built it, so a mismatched ultralytics or
# torch raises on YOLO(...) with an error naming a missing attribute rather than a version
# difference.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pillow==11.1.0

torch==2.11.0
torchvision==0.26.0
numpy==2.1.3
ultralytics==8.4.150
