# Mock mode only - no torch, no ultralytics.
# Enough to run the API with RICE_MOCK=1 and develop the Flutter client against it.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pillow==11.1.0
